"""Dataset loaders. Real public datasets from Hugging Face are the default; the only
synthetic text in the repo is (a) 200-row fixtures for unit tests and (b) the `draft`
router intent, both labelled as such."""
