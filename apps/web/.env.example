# Copy to .env.local (dev) or set in the Vercel project settings.
NEXT_PUBLIC_API_BASE=https://<workspace>--model-spectrum-web.modal.run
NEXT_PUBLIC_DEMO_KEY=<the DEMO_KEY you stored as the Modal secret demo-key>
